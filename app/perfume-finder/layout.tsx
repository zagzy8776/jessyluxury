import { pageMetadata } from '@/lib/seo-metadata'

export const metadata = pageMetadata.perfumeFinder

export default function PerfumeFinderLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
